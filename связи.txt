1) Отправка сообщения
HTTP POST /notifications
    ↓
router
    ↓
handler.Create()
    ↓
service.Create()
    ↓
repository.Create()
    ↓
PostgreSQL -> nats.publisher -> notifications.new -> WSManager -> user -> api.read
                                                               -> Redis -> user -> api.read