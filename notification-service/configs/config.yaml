server:
  port: "8080"
  read_timeout: 5s
  write_timeout: 5s

database:
  max_open_conns: 20
  max_idle_conns: 10
  conn_max_lifetime: 1h

nats:
  subject_new: "notifications_new"
  subject_read: "notifications.read"
